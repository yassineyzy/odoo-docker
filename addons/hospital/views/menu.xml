<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- explicit list view definition -->
<!--
    <record model="ir.ui.view" id="hospital.list">
      <field name="name">hospital list</field>
      <field name="model">hospital.hospital</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
-->

    <!-- actions opening views on models -->

    <!-- <record model="ir.actions.act_window" id="action_hospital_patient">
      <field name="name">Patients</field>
      <field name="res_model">hospital.patient</field>
      <field name="view_mode">tree,form</field>
      <field name="help" type="html">
        <p class="o_view_nocontent_smiling_face">
          Create your first patient!
        </p>
      </field>
    </record> -->


    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="hospital.action_server">
      <field name="name">hospital server</field>
      <field name="model_id" ref="model_hospital_hospital"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->

    <menuitem name="Hospital 123" id="hospital_menu_root" sequence="0"/>

    <menuitem name="Patient details" id="hospital_patient_details" parent="hospital_menu_root"/>


</odoo>
